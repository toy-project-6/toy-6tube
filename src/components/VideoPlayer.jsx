import React from 'react';
import YouTube from 'react-youtube';

const VideoPlayer = ({ videoId, size }) => {
  return (
    <YouTube
      videoId={''}
      id={''}
      className={''}
      iframeClassName={''}
      style={{}}
      title={''}
      loading={undefined}
      opts={size}
    />
  );
};

export default VideoPlayer;
